# these code snippets are taken from other personal projects. i am saving these for future access. these are not for public. so i wont give any detail about these.

import hashlib

mnemonic = input("Enter mnemonic: ")

salted_hash_base = mnemonic
for _ in range(int(((hashlib.sha256(mnemonic.encode()).hexdigest())[:2]),16)):
  salted_hash = hashlib.sha256(salted_hash_base.encode()).hexdigest()
  salted_hash_base = salted_hash


print(salted_hash_base)
